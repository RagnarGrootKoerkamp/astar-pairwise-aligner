[package]
name = "pairwise-alignment"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bio-types = "*"
bio = "*"
petgraph = ">=0.6"
rand = "*"
rand_chacha = "*"
# TODO: Only used for generating random sequences. Replace with own/more generic standalone implementation?
block-aligner={version="*", features=["simd_avx2"]}
arrayvec = "*"
csv = "*"
serde = "*"
itertools = "*"
strum_macros = "*"
